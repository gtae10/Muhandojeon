# 설치:  pip install -r requirements.txt

# 에이전트 응답 생성
#
# openai 는 상한을 둔다.
# >=2.0.0 만 적어두면 지금 새로 설치할 때 3.x 가 깔리는데, 그 버전으로는
# 한 번도 돌려본 적이 없다. 메이저 버전이 바뀌면 client.chat.completions.create,
# RateLimitError, response_format 중 무엇이 달라졌을지 알 수 없다.
# 팀원이 처음 설치하는 자리에서 깨지는 것이 가장 나쁘다.
# 3.x 로 올릴 때는 회귀를 다시 돌리고 이 줄을 고칠 것.
openai>=2.0.0,<3.0.0
python-dotenv>=1.0.0

# 팀 연동용 API 서버 (uvicorn api:app --reload --port 8000)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 제품 이미지 확인용 (선택 — 없어도 에이전트는 동작한다)
pillow>=11.0.0

# ─────────────────────────────────────────────────────────────
# 검증한 조합 (2026-08-16, 회귀 11종 통과)
#
#   openai    2.37.0
#   fastapi   0.141.1
#   pydantic  2.13.4
#   python    3.x
#
# 문제가 생기면 이 조합과 비교할 것.
#
# AI 1 파트(xgboost·scikit-learn·pandas·numpy·matplotlib·joblib·ucimlrepo)와
# 같은 환경에 설치해도 충돌하지 않는 것을 확인했다. (2026-08-16, pip dry-run)
# Backend 가 두 파트를 한 프로세스에서 돌려도 된다.
